//Handleing the API requests from TMBD

import { createContext } from "react"
import { BaseAxios } from "./BaseAxios"

export const ApiContext = createContext("")

export const ApiProvider = ({ children }) => {
    
}